namespace BookShoppingCartMvcUI.Constants
{
    public enum Roles
    {
        Admin,
        User = 1
    }
}
//القيم في enum بتكون عبارة عن int من الداخل (بس ممكن تحولها لـ string وقت الاستخدام).

